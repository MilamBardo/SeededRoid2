<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talanath.seededroid2">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />
    //stay with me

    <!-- <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="18"/> -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- <uses-feature -->
    <!-- android:name="android.hardware.camera" -->
    <!-- android:required="true" /> -->
    <uses-feature android:name="android.hardware.camera2.full" />

    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CaptureImageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CaptureImageActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GardensViewActivity" />
        <activity android:name=".AddGardenActivity" />
        <activity android:name=".GardenSeedsActivity" />
        <activity android:name=".ThoughtSeedViewActivity" />
        <activity android:name=".ImageSeedViewActivity" />
        <activity android:name=".TreeActivity" />
        <activity android:name=".TreeAddSceneActivity" />
        <activity android:name=".ErrorHandlerActivity" />
        <activity android:name=".TreesViewActivity" />
        <activity
            android:name=".TreePlayActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ImageSeedTakePhotoActivity" />
        <activity android:name=".SeedAddActivity" />
        <activity android:name=".InkSeedAddActivity" />
        <activity android:name=".MySeedsActivity" />
        <activity android:name=".SceneRequestActivity" />
        <activity android:name=".SceneRequestImageActivity" />
        <activity android:name=".SceneRequestInkActivity" />
        <activity android:name=".SceneMakePhotoActivity"></activity>
    </application>

</manifest>